# Runtime dependencies, pinned for reproducible builds.
# Bump deliberately (Dependabot opens PRs for these).
pillow==12.3.0
piexif==1.1.3
flask==3.1.3
werkzeug==3.1.5

# Optional: HEIC/HEIF (iPhone) support. Install with:
#   pip install -r requirements.txt pillow-heif==1.1.1
# Without it HEIC uploads are rejected with a clear error.
